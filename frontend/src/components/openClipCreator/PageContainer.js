// React
import React from "react";

// CSS
import "./openClipCreator.css";

const PageContainer = ({ children }) => {
  return <div className="oc-page-container">{children}</div>;
};

export default PageContainer;
